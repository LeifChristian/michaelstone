# Lyrics

Plain text files — one song per file. Edit these directly; no docx needed.

Credits are intentionally omitted from the site.

After changing a file, run `npm run scan` or just `npm run deploy`.
